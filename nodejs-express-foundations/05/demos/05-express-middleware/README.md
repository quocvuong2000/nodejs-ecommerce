# How to run this code

1. [Install Node.js](https://nodejs.org/en/download/package-manager/current).
2. Run the command `npm install` in the same directory that contains this `package.json` file.
3. Start the application by typing `node src/app-express.mjs`.